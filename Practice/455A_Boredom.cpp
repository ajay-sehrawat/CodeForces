#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    scanf("%d", &t);

    int a[t];
    for ( int i=0; i<t; i++ )
    {
        cin>>a[i];
    }
    int freq[t];
    for ( int i = 0; i<t; i++ )
    {
        for ( int j=0; j<t; j++ )
        {
            
        }
    }

    return 0;
}